#include "main.h"

/**
 * Description: add - adds the values.
 * @a: value copied to the function.
 * @b:value copied to the function
 * Return: (a+b)the result is returned to the main function
 */

int add(int a, int b)
{
	return (a + b);
}
